{application, starbound_support,
    [{vsn, "0.2.0"},
        {description, "This is the starbound support to maintain sb server."},
        {registered, []},
        {applications, [kernel, stdlib, recon, cowboy, elib]},
        {mod, {starbound_support_app, ["starbound_support"]}},
        {env, []}]}.